package com.fnts.habit;

public enum HabitType {
    /** Acquiring a good habit: "did you do it?" */
    BUILD,
    /** Removing a bad habit: "did you avoid it?" — same mechanics, inverted wording. */
    QUIT
}
